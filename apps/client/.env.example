# Client environment template. Copy to `.env.local` to override per machine.
# Only VITE_-prefixed vars are exposed to client code; never store secrets here.

# Base URL of the management API the dashboard calls.
VITE_API_BASE=http://localhost:5001/api/v1
