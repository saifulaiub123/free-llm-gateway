# Client environment — DEVELOPMENT (loaded by `vite dev`).
# Only VITE_-prefixed vars are exposed to client code; never put secrets here (they ship to the browser).
VITE_API_BASE=http://localhost:5001/api/v1
