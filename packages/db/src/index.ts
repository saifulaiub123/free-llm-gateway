/**
 * Public barrel for the `@gateway/db` package.
 *
 * Placeholder created in TASK-001. The table factory (TASK-004), connection
 * factory (TASK-005), schema, and migrations are added in later Phase 0 tasks.
 */
export {};
